/*
$(ocument).ready(function() {
  // Algunos colores suaves característicos de altopolimero
  var $naranja = '#ffe6d5';
  var $azul = '#d5f6ff';
  var $gris = '#e6e6e6';
  var x = 1;

  var navbar = $('.colortochange');

  function change() {
    if (x == 1) {
      $(navbar).css('background-color', $naranja);
      x = 2;
    } else if (x == 2) {
      $(navbar).css('background-color', $azul);
      x = 3;
    } else {
      $(navbar).css('background-color', $gris);
      x = 1;
    }
  }

  setInterval(change, 5000);

});
*/
